import { Outlet } from '@tanstack/react-router'


const GuestLayout = () => {
    return (
        <div>
            GuestLayouts
            <Outlet />
        </div>
    )
}

export default GuestLayout